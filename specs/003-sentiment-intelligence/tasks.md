# Tasks: Sentiment Intelligence

**Created**: 2026-04-05
**Depends on**: `002-market-data-ingestion` (`@openclaw/market-data-ingestion` — pg pool, BullMQ instance); `001-advanced-context-memory` (`@openclaw/trading-context` — MemDir/Redis)
**Package**: `extensions/sentiment-intelligence/` as `@openclaw/sentiment-intelligence`

---

## Phase 1: Setup

**Purpose**: Scaffold the plugin package so all subsequent phases have a valid workspace package to build into. Nothing in Phase 2+ can land without a compilable package.json, manifest, and directory tree.

- [ ] T001 Initialize `extensions/sentiment-intelligence/` with `package.json` (`@openclaw/sentiment-intelligence`, ESM, `"type": "module"`; `dependencies`: `axios`, `bullmq`, `zod`, `ioredis`, `pg`; `devDependencies`: `vitest`, `@types/node`, `typescript`), `openclaw.plugin.json` (id: `sentiment-intelligence`, displayName: `Sentiment Intelligence`, empty `configSchema` stub, `openclaw.install.npmSpec`: `@openclaw/sentiment-intelligence`, `openclaw.channel.id`: `sentiment-intelligence`), `tsconfig.json` extending the repo root tsconfig, and `src/` subdirectory tree (`schema/`, `feeds/`, `sentiment/`, `news/`, `macro/`, `embedding/`, `db/migrations/`, `health/`, `tools/`)
- [ ] T002 [P] Create `index.ts` shell exporting a `definePluginEntry` stub (imports from `openclaw/plugin-sdk/plugin-entry`) and `runtime-api.ts` exporting an `OpenClawPluginApi` type alias — mirrors the pattern in `extensions/market-data-ingestion/runtime-api.ts`
- [ ] T003 [P] Create `src/api.ts` public exports surface re-exporting inferred Zod types (`SentimentSnapshot`, `MacroContext`, `NewsEvent`, `FeedAccuracyReport`) and the `IFeed` interface; and `src/feeds/types.ts` declaring `IFeed<T>` with `readonly feedId: string`, `readonly schedule: string` (cron expression), and `poll(): Promise<T>` (throws on unrecoverable error, logs and returns cached on transient)

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core schemas, DB migrations, pg client, and BullMQ queue factory that ALL user stories depend on. No feed implementation work can begin until DB migrations are authored and schemas are validated.

### Zod Schemas

- [ ] T004 Define `SentimentSnapshotSchema` and inferred `SentimentSnapshot` type in `src/schema/SentimentSnapshot.ts` — fields: `symbol` (string, "BTC" | "global"), `fearGreedScore` (number [0,1]), `fearGreedLabel` (enum: `extreme_fear | fear | neutral | greed | extreme_greed`), `twitterScore` (number [0,1]), `tweetVolume` (int), `redditScore` (number [0,1]), `redditPostVolume` (int), `fundingBias` (enum: `long | short | neutral`), `fundingRate` (number, raw rate value), `compositeScore` (number [0,1]), `lastUpdated` (ISO string); write colocated test in `src/schema/SentimentSnapshot.test.ts` verifying valid payload parses, missing required field throws ZodError, numeric fields reject strings
- [ ] T005 [P] Define `MacroContextSchema` and inferred `MacroContext` type in `src/schema/MacroSnapshot.ts` — fields: `dxy` (number), `us10y` (number), `m2Supply` (number), `oilPriceWti` (number), `globalMarketCap` (number), `btcDominance` (number), `fomcNextDate` (string nullable), `fomcLastAction` (enum: `hold | cut | hike`, nullable), `cpiLastReading` (number nullable), `cpiNextDate` (string nullable), `regime` (enum: `risk_on | risk_off | neutral | uncertain`), `lastUpdated` (ISO string); colocated test verifies valid parse, nullable fields accept null, invalid regime value rejected
- [ ] T006 [P] Define `NewsEventSchema` and inferred `NewsEvent` type in `src/schema/NewsEvent.ts` — fields: `id` (number int), `headline` (string), `source` (string), `url` (string), `sentiment` (enum: `positive | negative | neutral`), `impactClass` (enum: `regulatory | macro | technical | hack | institutional | other`), `classificationConfidence` (number [0,1]), `symbols` (array of strings), `publishedAt` (ISO string); colocated test verifies extra fields are stripped and enum values are enforced
- [ ] T007 [P] Define `FeedAccuracyReportSchema` and inferred `FeedAccuracyReport` type in `src/schema/FeedAccuracy.ts` — fields: `feeds` (array of `{ feedId: string, lastSuccessfulPoll: string, isStale: boolean, accuracy30d: number nullable, sampleCount: number int, weight: number }`); colocated test verifies null `accuracy30d` is valid and `weight` rejects negative values

### Database Infrastructure

- [ ] T008 Write migration SQL in `src/db/migrations/001_initial.sql` — (1) `sentiment_snapshots` TimescaleDB hypertable partitioned on `timestamp` (columns: `symbol text`, `timestamp timestamptz`, `fear_greed_score real`, `twitter_score real`, `reddit_score real`, `funding_bias text`, `funding_rate real`, `composite_score real`, `regime text`); (2) `news_events` hypertable (`id BIGSERIAL`, `headline text`, `source text`, `url text`, `symbols text[]`, `impact_class text`, `sentiment text`, `classification_confidence real`, `published_at timestamptz`, `ingested_at timestamptz DEFAULT NOW()`; unique dedup index on `(lower(regexp_replace(headline, '[^a-z0-9 ]', '', 'gi')), date_trunc('5 minutes', published_at))`); (3) `macro_snapshots` regular table (columns: `id BIGSERIAL PRIMARY KEY`, `series_id text NOT NULL`, `value float8 NOT NULL`, `unit text NOT NULL`, `effective_date date NOT NULL`, `fetched_at timestamptz DEFAULT NOW()`; unique constraint on `(series_id, effective_date)`); (4) `sentiment_embeddings` pgvector table (`id bigserial PRIMARY KEY`, `type text`, `symbol text`, `timestamp timestamptz`, `regime text`, `text_chunk text`, `embedding vector(768)`, `outcome text`, `pnl_pct real`; HNSW index on `embedding`; unique on `(type, timestamp, symbol)`); (5) `feed_accuracy` table (`feed_name text`, `correct_predictions int`, `total_predictions int`, `window_start timestamptz`, `window_end timestamptz`; primary key `(feed_name, window_start)`)
- [ ] T009 Implement `pg` Pool singleton in `src/db/client.ts` — lazy-init on first use, connection URL from plugin config (`postgresUrl`), exports `getPool(): Pool` and `closePool(): Promise<void>`, and typed query helper `query<T>(sql: string, params?: unknown[]): Promise<T[]>`; log connection errors at `error` level without logging the URL string; run `src/db/migrations/001_initial.sql` via `runMigrations(pool): Promise<void>` export that idempotently executes the migration SQL using `IF NOT EXISTS` guards
- [ ] T010 [P] Implement BullMQ queue factory in `src/infra/queues.ts` — `createSentimentQueue(redisClient: Redis): Queue` returns `trading:sentiment:poll` queue with `defaultJobOptions: { attempts: 3, backoff: { type: "exponential", delay: 5000 } }` and max delay 60 s; `createEmbedQueue(redisClient: Redis): Queue` returns `sentiment:embed` queue with `defaultJobOptions: { attempts: 5, backoff: { type: "exponential", delay: 5000 } }` and max delay 300 s; fall back to in-process queue stub with `warn` log if Redis is unavailable at creation time

**Checkpoint**: Schemas validated, migration SQL authored, pg client ready, BullMQ wired — feed implementations can now begin.

---

## Phase 3: User Story 1 — Sentiment Feeds + Aggregation + `get_sentiment` (Priority: P1) 🎯 MVP

**Goal**: FearGreed, Reddit, and Twitter poll workers write sub-scores to MemDir every 4 h; `SentimentAggregator` reads all sub-scores and writes a composite `SentimentSnapshot` to `sentiment:composite:{symbol}` (TTL: 3600 s); `get_sentiment()` tool reads from MemDir only and resolves within 100 ms.

**Independent Test**: Plugin running with at least one poll cycle complete; call `get_sentiment("BTC")`; response includes `fearGreedScore` in [0,1], `twitterScore` in [0,1], `redditScore` in [0,1], `fundingBias` (`long | short | neutral`), `compositeScore` in [0,1], and `lastUpdated` no older than 5 hours; entire call resolves within 100 ms.

- [ ] T011 [US1] Implement `FearGreedFeed` in `src/feeds/FearGreedFeed.ts` — `feedId: "fear_greed"`, `schedule: "0 */4 * * *"`; `poll()` sends `GET https://api.alternative.me/fng/?limit=1` via `axios` with a 10 s timeout; validates response with Zod (`{ data: [{ value: z.string(), value_classification: z.string() }] }`); normalizes `score = Number(value) / 100` (clamps to [0,1], emits `warn` if clamping fires); maps `value_classification` to the `fearGreedLabel` enum; writes `{ score, label: fearGreedLabel, lastUpdated: new Date().toISOString() }` to MemDir key `sentiment:subfeed:fear_greed` with TTL 14400 s; on success writes health key `sentiment:health:fear_greed` with `{ lastSuccessfulPoll, isStale: false }`; write colocated test in `src/feeds/FearGreedFeed.test.ts` — mocked axios returns a valid payload → score written to MemDir correctly; invalid payload → ZodError logged, poll rejects; score out of [0,1] → clamped and warn logged
- [ ] T012 [P] [US1] Implement `RedditFeed` in `src/feeds/RedditFeed.ts` — `feedId: "reddit"`, `schedule: "0 */4 * * *"`; `poll(symbol: string)` sends `GET https://www.reddit.com/r/CryptoCurrency/hot.json?limit=25` and `GET https://www.reddit.com/r/Bitcoin/hot.json?limit=25` via `axios` (no auth); filters posts whose title or flair mentions `symbol` (case-insensitive); scores each post title against keyword lists (bullish: `["bull", "moon", "pump", "rally", "ath", "long"]`; bearish: `["bear", "dump", "crash", "short", "rekt", "capitulation"]`); rolling 4 h mean of per-post scores normalized to [0,1] (0.5 = neutral); writes `{ score, postVolume: filteredPostCount, lastUpdated }` to MemDir key `sentiment:subfeed:reddit:{symbol}` with TTL 14400 s; health key `sentiment:health:reddit`; colocated test in `src/feeds/RedditFeed.test.ts` — mocked axios response with mixed bullish/bearish titles → score between 0.5 and 1; symbol not mentioned → score 0.5; empty subreddit response → score 0.5, postVolume 0
- [ ] T013 [P] [US1] Implement `TwitterFeed` in `src/feeds/TwitterFeed.ts` — `feedId: "twitter"`, `schedule: "0 */4 * * *"`; if `twitterBearerToken` config is absent, emits a single `warn` log at startup and sets `isDisabled = true`, `poll()` resolves with a neutral stub `{ score: 0.5, tweetVolume: 0 }` without making network calls; when token is present, sends `GET https://api.twitter.com/2/tweets/search/recent?query={symbol}+crypto+lang:en&max_results=100` with `Authorization: Bearer {token}`; if API returns 429 or 403, gracefully disables feed (sets `isDisabled = true`, logs `warn`, marks stale); scores each tweet text against the same keyword lists as `RedditFeed`; rolling 4 h mean normalized to [0,1]; writes to MemDir key `sentiment:subfeed:twitter:{symbol}` with TTL 14400 s; if `nitterBaseUrl` config is set, falls back to `GET {nitterBase}/{symbol}/search.json` before disabling; colocated test in `src/feeds/TwitterFeed.test.ts` — no bearer token → resolves with stub score; 429 response → feed disables and returns stub on subsequent poll calls; valid response → score derived correctly from keyword counts
- [ ] T014 [US1] Implement funding-bias derivation in `src/sentiment/funding-bias.ts` — `deriveFundingBias(symbol: string, memDir: MemDir): Promise<{ bias: "long" | "short" | "neutral", rate: number }>` reads the key `sentiment:{exchange}:funding_bias:{symbol}` (TTL 600 s) from MemDir (falling back to `{exchange}:funding:{symbol}` written by `002-market-data-ingestion`); if key is expired or absent returns `{ bias: "neutral", rate: 0 }`; interprets `rate > 0.00001` as `"long"` (longs pay), `rate < -0.00001` as `"short"` (shorts pay), else `"neutral"`; colocated test in `src/sentiment/funding-bias.test.ts` — rate 0.012% → `"long"`; rate −0.005% → `"short"`; absent key → `"neutral"`; magnitude exactly 0.00001 → `"neutral"`
- [ ] T015 [US1] Implement `SentimentAggregator` in `src/sentiment/aggregator.ts` — `aggregate(symbol: string, memDir: MemDir, pool: Pool): Promise<SentimentSnapshot>` reads sub-feed MemDir keys (`sentiment:subfeed:fear_greed`, `sentiment:subfeed:twitter:{symbol}`, `sentiment:subfeed:reddit:{symbol}`) and calls `deriveFundingBias`; checks TTL freshness by parsing the `lastUpdated` field against `Date.now() - 5 * 60 * 60 * 1000` (5 h stale threshold); for each stale feed sets its weight to 0 and redistributes proportionally among healthy feeds (default weights F&G 30%, Twitter 30%, Reddit 30%, Funding 10%); computes `compositeScore = Σ(weight_i × score_i)` clamped to [0,1] with `warn` log if clamping fires; writes `SentimentSnapshot` JSON to MemDir key `sentiment:composite:{symbol}` with TTL 3600 s and inserts a row into `sentiment_snapshots` via write queue; after per-symbol aggregation, also computes and writes a global composite to `sentiment:composite:global` (using Fear & Greed score as the global base plus the average of per-symbol funding biases, clamped to [0,1]); enqueues a `sentiment:embed` BullMQ job `{ type: "sentiment", symbol, timestamp }` after successful DB insert
- [ ] T016 [P] [US1] Write tests for `SentimentAggregator` in `src/sentiment/aggregator.test.ts` — all four feeds present → weighted composite computed correctly; one feed stale → weight redistributed to remaining three (weights still sum to 1.0); all feeds stale → composite 0.5 and `warn` logged; composite pre-clamp value > 1 → clamped to 1 and `warn` logged; mock MemDir and pg pool — DB insert called with correct column values; BullMQ enqueue called once per aggregate call; correct symbol passed through all sub-feed keys
- [ ] T017 [US1] Implement `get-sentiment` tool handler in `src/tools/get-sentiment.ts` — accepts `(symbol?: string)`; if `symbol` omitted reads `sentiment:composite:global`, else reads `sentiment:composite:{symbol}`; calls `memDir.get(key)` and parses result with `SentimentSnapshotSchema`; returns the parsed snapshot directly on cache hit; returns typed error `{ error: "not_found" }` if key is absent (TTL expired or no poll cycle yet); entire function must contain no network or DB calls (hot path: single Redis GET only); register tool signature `get_sentiment(symbol?: string) → SentimentSnapshot | { error: "not_found" }`

**Checkpoint**: US1 fully testable — poll workers → MemDir sub-feed keys → aggregator → `sentiment:composite:{symbol}` → tool response.

---

## Phase 4: User Story 2 — Macro Scheduler + FRED + Regime Classifier + `get_macro_context` (Priority: P1)

**Goal**: `FredFeed` pulls DXY, US10Y, M2, WTI daily via the FRED API; `MacroScheduler` upserts `macro_snapshots` rows and builds a `MacroContext` written to `macro:snapshot` (TTL: 86400 s); `get_macro_context()` resolves within 200 ms from MemDir.

**Independent Test**: Plugin running with one macro pull completed; call `get_macro_context()`; response includes non-zero `dxy`, `us10y`, `m2Supply`, `oilPriceWti`, `globalMarketCap`, `btcDominance`, a `regime` value, and `lastUpdated` within 26 hours; entire call resolves within 200 ms.

- [ ] T018 [US2] Implement `FredFeed` in `src/feeds/FredFeed.ts` — `feedId: "fred"`, `schedule: "0 6 * * *"` (daily 06:00 UTC); `poll()` fires four parallel `axios` requests to `https://api.stlouisfed.org/fred/series/observations?series_id={id}&api_key={fredApiKey}&sort_order=desc&limit=1&file_type=json` for series `DTWEXBGS` (DXY), `DGS10` (US10Y), `M2SL` (M2 in billions USD), `DCOILWTICO` (WTI oil USD/bbl); also `GET https://pro-api.coinmarketcap.com/v1/global-metrics/quotes/latest` for `globalMarketCap` and `btcDominance` (requires `coinMarketCapApiKey`); validates each FRED response with Zod (`{ observations: [{ value: z.string(), date: z.string() }] }`), rejects "." placeholder values (FRED uses "." for missing data — treat as null); returns `{ dxy, us10y, m2Supply, oilPriceWti, globalMarketCap, btcDominance, effectiveDate }` — all nullable for individual series failures; colocated test in `src/feeds/FredFeed.test.ts` — mocked axios: all series return valid values → all fields populated; one series returns "." → that field is null, others still populated; CMC API key absent → warn logged, `globalMarketCap` and `btcDominance` null
- [ ] T019 [P] [US2] Implement `MacroScheduler` in `src/macro/MacroScheduler.ts` — `register(queue: Queue): void` registers four BullMQ `RepeatableJob`s: (1) `fred-daily` at `schedule: "0 9 * * *"` calling `FredFeed.poll()`; (2) `fomc-weekly` at `schedule: "0 9 * * 1"` scraping `https://www.federalreserve.gov/monetarypolicy/fomccalendars.htm` with `axios` + simple HTML parse for next meeting date and last action (regex for `hold | cut | hike`); (3) `cpi-weekly` at `schedule: "0 9 * * 1"` scraping the BLS CPI release calendar (`https://www.bls.gov/schedule/news_release/cpi.htm`); on FOMC or CPI HTML parse failure: retain last known values in MemDir, set the failed field(s) to `null`, log error at `error` level, and send an alert via `pluginApi.alert` with the parse error details; (4) `macro-context-build` which runs after each successful `fred-daily` or `fomc-weekly` job — calls `buildMacroContext(pool, memDir)` which: (a) reads the latest row per series from `macro_snapshots` via `DISTINCT ON (series_id) ORDER BY series_id, effective_date DESC`; (b) assembles partial `MacroContext` from per-series values; (c) applies `regimeClassifier` to produce the `regime` field; (d) writes the full `MacroContext` JSON to MemDir key `macro:snapshot` with TTL 86400 s; if any individual series has no rows, its field is `null` in the `MacroContext`; also writes health key `sentiment:health:fred`; colocated test in `src/macro/MacroScheduler.test.ts` — mock BullMQ queue: `register()` adds exactly 4 repeatable jobs with correct cron strings; `buildMacroContext` with risk-off thresholds → regime `risk_off`; missing FOMC data → `fomcNextDate: null` and `regime: "uncertain"`
- [ ] T020 [P] [US2] Implement `regimeClassifier` in `src/sentiment/regime-classifier.ts` — `classifyRegime(macro: Partial<MacroContext>): "risk_on" | "risk_off" | "neutral" | "uncertain"`; returns `"uncertain"` if `dxy`, `us10y`, or `fomcLastAction` are null; returns `"risk_off"` if `dxy > 104 AND us10y > 4.5 AND fomcLastAction === "hike"` (thresholds overridable via `regimeRules` config); returns `"risk_on"` if `dxy < 100 AND us10y < 3.5`; returns `"neutral"` otherwise; colocated test in `src/sentiment/regime-classifier.test.ts` — DXY 105, US10Y 4.8, last hike → `risk_off`; DXY 98, US10Y 3.2 → `risk_on`; all below risk-off thresholds → `neutral`; null DXY → `uncertain`; verify custom `regimeRules` overrides the default thresholds
- [ ] T021 [US2] Add macro DB query helpers to `src/db/queries.ts` — `upsertMacroSnapshot(pool, row: Omit<MacroContext, "regime" | "lastUpdated"> & { seriesId: string, effectiveDate: string, regime: string }): Promise<void>` upserts into `macro_snapshots` on `(series_id, effective_date)` conflict; `insertSentimentSnapshot(pool, row: SentimentSnapshot): Promise<number>` inserts into `sentiment_snapshots` and returns the new row `id`; `queryLatestMacroSnapshot(pool): Promise<Record<string, unknown> | null>` selects the most recent row per series via `DISTINCT ON (series_id) ORDER BY series_id, effective_date DESC`; write tests in `src/db/queries.test.ts` with a mocked pg Pool — verify correct SQL shape, ON CONFLICT clause, and that `queryLatestMacroSnapshot` returns `null` when table is empty
- [ ] T022 [P] [US2] Implement `get-macro-context` tool handler in `src/tools/get-macro-context.ts` — accepts no arguments; reads MemDir key `macro:snapshot` and parses with `MacroContextSchema`; returns the parsed `MacroContext` on cache hit; returns typed error `{ error: "not_found" }` if absent; contains no network or DB calls (hot path: single Redis GET only); register tool signature `get_macro_context() → MacroContext | { error: "not_found" }`; write colocated test in `src/tools/get-macro-context.test.ts` — mocked MemDir returns valid JSON → schema parses and returns correctly; mocked MemDir returns null → typed error returned; malformed JSON → error logged and typed error returned

**Checkpoint**: US2 fully testable — `MacroScheduler` jobs → `macro_snapshots` upsert → `buildMacroContext` → MemDir `macro:snapshot` → tool response.

---

## Phase 5: User Story 3 — News Feeds + Classifier + Deduplicator + `get_news_events` (Priority: P2)

**Goal**: `CryptoPanicFeed` polls every 30 min; classifier assigns `impactClass` and `sentiment`; deduplicator prevents duplicate rows in `news_events` hypertable; `get_news_events("BTC", 5)` returns up to 5 classified headlines from DB.

**Independent Test**: Plugin running with one ingestion cycle complete; call `get_news_events("BTC", 5)`; response is an array of ≤5 items each with `headline`, `source`, `sentiment`, `impactClass`, `symbols`, and `publishedAt`; a second round with the same CryptoPanic payload produces no new `news_events` rows (deduplication).

- [ ] T023 [US3] Implement `CryptoPanicFeed` in `src/feeds/CryptoPanicFeed.ts` — `feedId: "cryptopanic"`, `schedule: "*/30 * * * *"` (every 30 min); `poll(symbols: string[])` sends `GET https://cryptopanic.com/api/free/v1/posts/?auth_token={cryptoPanicApiKey}&public=true` via `axios` with a 15 s timeout; if `cryptoPanicApiKey` is absent, emits `warn` and returns `[]` without network call; validates response with Zod (`{ results: z.array(z.object({ title: z.string(), source: z.object({ title: z.string() }), url: z.string(), published_at: z.string(), currencies: z.array(z.object({ code: z.string() })).optional() })) }`); maps each result to a candidate news event; passes each to `deduplicator.isDuplicate()` — skips if true; passes non-duplicate candidates to `classifier.classify()` — gets `{ impactClass, sentiment, confidence }`; inserts rows into `news_events` via `insertNewsEvent()` query helper; colocated test in `src/feeds/CryptoPanicFeed.test.ts` — mocked axios: 3 results returned, 1 is a duplicate → exactly 2 inserts called; missing API key → returns [] without HTTP call
- [ ] T024 [US3] Implement `classifier` in `src/news/classifier.ts` — `classify(headline: string): Promise<{ impactClass: NewsEvent["impactClass"], sentiment: NewsEvent["sentiment"], confidence: number }>`; **Tier 1** (synchronous, no I/O): match against keyword sets — `regulatory`: `[/\b(SEC|CFTC|ban|law|regulation|lawsuit|fine)\b/i]`; `hack`: `[/\b(exploit|hack|stolen|breach|compromised|theft)\b/i]`; `institutional`: `[/\b(ETF|hedge fund|treasury|adoption|institutional|BlackRock|Fidelity)\b/i]`; `technical`: `[/\b(upgrade|fork|testnet|protocol|mainnet|migration)\b/i]`; `macro`: `[/\b(Fed|interest rate|CPI|GDP|inflation|FOMC|yield)\b/i]`; positive terms: `[/\b(approval|surge|rally|bullish|partnerships|record)\b/i]`; negative terms: `[/\b(crash|reject|ban|scam|fraud|collapse|plunge)\b/i]`; set `confidence: 0.9` for strong keyword hit (≥2 matching patterns), `0.65` for partial (1 match), default to `impactClass: "other"` and `sentiment: "neutral"` with `confidence: 0.5` when no match; **Tier 2** (async, only when Tier 1 `confidence < 0.6`): POST `{ model: ollamaClassifyModel, prompt: "Classify this crypto headline: ..." }` to `{ollamaBaseUrl}/api/generate`; parse JSON response; on parse failure or Ollama unavailability use Tier 1 result as-is; colocated test in `src/news/classifier.test.ts` — "Bitcoin ETF approval" → `impactClass: "institutional"`, `sentiment: "positive"`; "Ethereum exploit drains $50M" → `impactClass: "hack"`, `sentiment: "negative"`; ambiguous headline → Tier 2 Ollama mock called; Ollama unreachable → Tier 1 result returned
- [ ] T025 [P] [US3] Implement `deduplicator` in `src/news/deduplicator.ts` — `isDuplicate(headline: string, publishedAt: string, pool: Pool): Promise<boolean>` normalizes the headline to lowercase and strips punctuation; computes a fingerprint `normalized_title + ":" + date_trunc_5min(publishedAt)`; queries `SELECT 1 FROM news_events WHERE lower(regexp_replace(headline, '[^a-z0-9 ]', '', 'gi')) = $1 AND date_trunc('5 minutes', published_at) = $2 LIMIT 1`; returns `true` if a row exists; colocated test in `src/news/deduplicator.test.ts` — mocked pg Pool returns a row for known headline → `true`; unknown headline → `false`; different 5-min bucket same headline → `false`
- [ ] T026 [P] [US3] Add news DB query helper to `src/db/queries.ts` — `insertNewsEvent(pool, event: Omit<NewsEvent, "id">): Promise<void>` inserts into `news_events` using `ON CONFLICT` on the dedup unique index `DO NOTHING`; `queryNewsEvents(pool, opts: { symbol?: string, limit?: number }): Promise<NewsEvent[]>` selects from `news_events` with optional `WHERE symbols @> ARRAY[$1::text]` filter and `ORDER BY timestamp DESC LIMIT $n` (default limit 10, cap 50); map rows to `NewsEventSchema`; write tests in `src/db/queries.test.ts` — mocked pool: verify `@>` array operator in symbol filter SQL; verify LIMIT applied and capped at 50; verify `ON CONFLICT DO NOTHING` in insert
- [ ] T027 [US3] Implement `get-news-events` tool handler in `src/tools/get-news-events.ts` — accepts `(symbol?: string, limit?: number)` where `limit` defaults to `newsDefaultLimit` config (default 10) and is capped at `newsMaxLimit` config (default 50); calls `queryNewsEvents(pool, { symbol, limit })`; returns `NewsEvent[]` (may be empty array — never returns a typed error, returns `[]` if no events ingested yet); resolves within 500 ms p99 (DB query, no MemDir hop); register tool signature `get_news_events(symbol?: string, limit?: number) → NewsEvent[]`

**Checkpoint**: US3 fully testable — CryptoPanic poll → dedup → classifier → `news_events` insert → `get_news_events` tool response.

---

## Phase 6: User Story 4 — Serializer + Embedding Pipeline + pgvector (Priority: P2)

**Goal**: Async BullMQ worker serializes each `SentimentSnapshot` and `MacroContext` to a text chunk, POSTs to Ollama `nomic-embed-text`, and upserts a 768-dim vector into `sentiment_embeddings`; hot path reads (`get_sentiment`, `get_macro_context`) are never blocked.

**Independent Test**: After one poll cycle, query `sentiment_embeddings` directly — rows present with `embedding` length 768 and correct `type` and `symbol` metadata; `get_sentiment("BTC")` remains ≤100 ms even when Ollama is paused (embedding worker retrying).

- [ ] T028 [US4] Implement `serializer` in `src/embedding/serializer.ts` — `serializeSentiment(snapshot: SentimentSnapshot): string` produces a human-readable text chunk, e.g.: `"Sentiment snapshot {timestamp} {symbol}: Fear & Greed: {fearGreedScore} ({fearGreedLabel}). Twitter score: {twitterScore} ({tweetVolume} tweets). Reddit score: {redditScore} ({redditPostVolume} posts). Funding bias: {fundingBias} (rate: {fundingRate*100}%). Composite: {compositeScore}. Regime: {regime}."` (all numbers formatted to 4 decimal places); `serializeMacro(macro: MacroContext): string` produces a parallel text chunk for macro fields; colocated test in `src/embedding/serializer.test.ts` — verify output is a non-empty string containing all field values; verify `undefined` or `null` optional fields are omitted gracefully
- [ ] T029 [US4] Implement `EmbeddingPipeline` BullMQ worker in `src/embedding/EmbeddingPipeline.ts` — `start(queue: Queue, pool: Pool, config: { ollamaBaseUrl: string, ollamaEmbedModel: string }): Worker` creates a BullMQ Worker on the `sentiment:embed` queue; for each job `{ type: "sentiment" | "macro", symbol, timestamp }`: (1) reads the snapshot row from `sentiment_snapshots` or `macro_snapshots` by `timestamp + symbol`; (2) calls `serializeSentiment` or `serializeMacro`; (3) POSTs to `{ollamaBaseUrl}/api/embeddings` with `{ model: ollamaEmbedModel, prompt: textChunk }`; validates response with Zod (`{ embedding: z.array(z.number()).length(768) }`); (4) upserts into `sentiment_embeddings` on `(type, timestamp, symbol)` conflict `DO NOTHING`; exponential backoff retry policy: `attempts: 5`, base 5 s, cap 300 s; on Ollama unavailability logs `warn` and retries — never throws or blocks the poll worker; colocated test in `src/embedding/EmbeddingPipeline.test.ts` — mock Ollama returns 768-dim vector → DB upsert called; Ollama 500 → job retried, no DB call; duplicate job for same `(type, timestamp, symbol)` → `DO NOTHING` upsert called correctly (mock pg returns rowCount 0, no error)
- [ ] T030 [P] [US4] Add embedding DB query helper to `src/db/queries.ts` — `upsertEmbedding(pool, row: { type: string, symbol: string, timestamp: string, regime: string, textChunk: string, embedding: number[], outcome?: string, pnlPct?: number }): Promise<void>` upserts into `sentiment_embeddings` on `(type, timestamp, symbol)` conflict `DO NOTHING`; `queryEmbeddingsBySymbol(pool, symbol: string, limit?: number): Promise<Array<{ id: number, type: string, timestamp: string, regime: string }>>` returns embedding metadata (not vectors) sorted by `timestamp DESC`; write tests verifying the `vector(768)` cast syntax in the INSERT statement and that the conflict target matches the HNSW-indexed unique constraint
- [ ] T031 [P] [US4] Write colocated integration test in `src/embedding/EmbeddingPipeline.test.ts` (extend existing) — mock MemDir and TimescaleDB; push a synthetic `SentimentSnapshot` through `SentimentAggregator` → verify `sentiment:embed` BullMQ job enqueued with correct `{ type: "sentiment", symbol, timestamp }`; simulate `EmbeddingPipeline` processing that job → verify `upsertEmbedding` called; simulate second job with identical key → verify only one `upsertEmbedding` call with rowCount 0 (idempotency); verify `get_sentiment()` reads from MemDir exclusively and is not affected by EmbeddingPipeline delay

**Checkpoint**: US4 fully testable — snapshot written → BullMQ embed job enqueued → Ollama call → pgvector upsert; hot path unaffected when Ollama is down.

---

## Phase 7: User Story 5 — Health Monitor + Accuracy Scorer + `get_feed_accuracy` (Priority: P3)

**Goal**: `HealthMonitor` cron runs every 5 min and marks stale feeds; `AccuracyScorer` appends accuracy rows on trade outcomes; `get_feed_accuracy()` returns per-feed staleness + 30-day accuracy stats.

**Independent Test**: With two poll cycles completed, call `get_feed_accuracy()`; response includes entries for `fear_greed`, `twitter`, `reddit`, and `funding_bias`; each entry has `feedId`, `lastSuccessfulPoll` ISO string, `isStale: false` (polls completed), `accuracy30d: null` (fewer than 10 trades scored), `sampleCount: 0`, `weight: 1.0`.

- [ ] T032 [US5] Implement `HealthMonitor` in `src/health/HealthMonitor.ts` — `start(queue: Queue, memDir: MemDir, pluginApi: OpenClawPluginApi): Worker` creates a BullMQ Worker on a `sentiment:health` repeatable job (`schedule: "*/5 * * * *"`); for each registered feed (`fear_greed`, `twitter`, `reddit`, `cryptopanic`, `fred`): (1) reads `sentiment:health:{feedId}` from MemDir; (2) computes `isStale = (Date.now() - new Date(lastSuccessfulPoll).getTime()) > 2 × scheduledIntervalMs` (where scheduled intervals: fear_greed/twitter/reddit = 4 h, cryptopanic = 30 min, fred = 24 h); (3) if `isStale` transitions from `false → true`: emits `warn` log `"Feed {feedId} is stale: last polled {staleDurationMinutes} minutes ago"` and calls `pluginApi.alert({ channelId: alertChannelId, message: "..." })`; (4) if `isStale` transitions from `true → false` (recovery): emits `info` log; (5) writes updated health record to MemDir key `sentiment:health:{feedId}` preserving `lastSuccessfulPoll` and updating `isStale`; colocated test in `src/health/HealthMonitor.test.ts` — feed last polled 9 h ago (> 2× 4 h interval) → `isStale: true`, warn logged, `pluginApi.alert` called; previously stale feed with fresh `lastSuccessfulPoll` → `isStale: false`, info logged; feed with no health key → treated as stale immediately
- [ ] T033 [P] [US5] Implement `AccuracyScorer` in `src/health/AccuracyScorer.ts` — `scoreOutcome(tradeId: string, outcome: "profit" | "loss", pool: Pool): Promise<void>` (called by Phase 5 execution hook — passive until then); for each active sentiment feed, queries `sentiment_snapshots` for the entry closest to the trade entry timestamp; compares predicted direction (composite score > 0.5 = bullish → profit expected) against `outcome`; inserts a row into `feed_accuracy` with `{ feedName, correctPredictions: wasCorrect ? 1 : 0, totalPredictions: 1, windowStart: startOfDay, windowEnd: endOfDay }`; `queryFeedAccuracy(pool, feedName: string, days: number): Promise<{ accuracy30d: number | null, sampleCount: number }>` aggregates `SUM(correct_predictions)` and `COUNT(*)` over the past `days` days; returns `accuracy30d: null` if `sampleCount < 10`; colocated test in `src/health/AccuracyScorer.test.ts` — mock pool: 3 profitable trades, all sentiment composite > 0.5 → insert called 3 times with `correctPredictions: 1`; 8 scored trades → `accuracy30d: null` (below threshold); 10+ trades, 7 correct → `accuracy30d: 0.7`
- [ ] T034 [P] [US5] Add `feed_accuracy` DB query helper to `src/db/queries.ts` — `insertFeedAccuracyRow(pool, row: { feedName: string, correctPredictions: number, totalPredictions: number, windowStart: string, windowEnd: string }): Promise<void>` with `ON CONFLICT (feed_name, window_start) DO UPDATE SET correct_predictions = feed_accuracy.correct_predictions + EXCLUDED.correct_predictions, total_predictions = feed_accuracy.total_predictions + EXCLUDED.total_predictions`; write tests verifying the ON CONFLICT UPDATE accumulates counts correctly
- [ ] T035 [US5] Complete `get-feed-accuracy` tool handler in `src/tools/get-feed-accuracy.ts` — accepts no arguments; for each of the 5 known feed IDs (`fear_greed`, `twitter`, `reddit`, `cryptopanic`, `fred`): reads `sentiment:health:{feedId}` from MemDir for `lastSuccessfulPoll` and `isStale`; calls `queryFeedAccuracy(pool, feedId, 30)` for `accuracy30d` and `sampleCount`; derives `weight`: if `accuracy30d !== null` and `sampleCount >= 10`, compute `weight = 0.5 + accuracy30d` (linear normalization to [0.5, 1.5] range); else `weight = 1.0` (equal weighting until statistically significant sample); assembles `FeedAccuracyReport` and validates with `FeedAccuracyReportSchema`; reads may be parallel (`Promise.all`); resolves within 500 ms (2 Redis GETs + 1 DB query per feed, 5 feeds → batch); register tool signature `get_feed_accuracy() → FeedAccuracyReport`

**Checkpoint**: US5 fully testable — HealthMonitor detects stale feeds + alerts; AccuracyScorer scores outcomes; `get_feed_accuracy` returns live per-feed stats.

---

## Phase 8: Polish, Tool Registration & Plugin Entry

**Purpose**: Wire all components into the OpenClaw plugin lifecycle; register 4 tools; populate configSchema; write end-to-end integration tests.

### Tool Registration & Config

- [ ] T036 Register all 4 OpenClaw tools in `index.ts` via `definePluginEntry` using `openclaw/plugin-sdk/plugin-entry` — `get_sentiment`, `get_macro_context`, `get_news_events`, `get_feed_accuracy`; bind each registration to its handler from `src/tools/`; tool schemas must use `stringEnum` / `Type.Optional` patterns (no `anyOf` / `oneOf` per tool schema guardrail)
- [ ] T037 [P] Populate `openclaw.plugin.json` `configSchema` with all overridable knobs — `symbols: string[]` (default: `["BTC", "ETH"]`), `fearGreedIntervalCron: string` (default: `"0 */4 * * *"`), `twitterIntervalCron: string`, `redditIntervalCron: string`, `newsIntervalCron: string` (default: `"*/30 * * * *"`), `macroDailyCron: string` (default: `"0 9 * * *"`), `macroWeeklyCron: string` (default: `"0 9 * * 1"`), `compositeWeights: object`, `regimeRules: object`, `newsDefaultLimit: number` (default: 10), `newsMaxLimit: number` (default: 50), `alertChannelId: string`, `ollamaBaseUrl: string` (default: `"http://localhost:11434"`), `ollamaEmbedModel: string` (default: `"nomic-embed-text"`), `ollamaClassifyModel: string` (default: `"llama3.2"`), `symbolAliasMap: object`; SecretRef-typed credential fields (never logged): `twitterBearerToken`, `fredApiKey`, `coinMarketCapApiKey`, `cryptoPanicApiKey`, `postgresUrl`, `redisUrl`, `nitterBaseUrl`

### Plugin Entry Lifecycle

- [ ] T038 Complete `index.ts` `definePluginEntry` lifecycle — `onActivate(api)`: (1) read config, create `MemDir` client from `@openclaw/trading-context` via `createMemDir({ client: getRedisClient() })`; (2) call `runMigrations(pool)`; (3) create BullMQ queues via `createSentimentQueue` and `createEmbedQueue`; (4) instantiate feeds (`FearGreedFeed`, `RedditFeed`, `TwitterFeed`, `CryptoPanicFeed`, `FredFeed`) and register one BullMQ `RepeatableJob` per feed using the feed's `schedule` plus `SentimentAggregator` or `MacroScheduler` callback; (5) start `EmbeddingPipeline` worker; (6) start `HealthMonitor` worker; `onDeactivate`: (7) close all BullMQ workers gracefully (`await worker.close()`); (8) await `closePool()`; all BullMQ job IDs are idempotent (named `{feedId}:repeat`) so gateway restarts do not create duplicate repeatable jobs

### Integration Tests

- [x] T039 [P] Write integration test in `test/sentiment-intelligence.integration.test.ts` — **Fear & Greed → `get_sentiment`**: mock `axios` to return `{ data: [{ value: "72", value_classification: "Greed" }] }`; invoke `FearGreedFeed.poll()`; assert MemDir key `sentiment:subfeed:fear_greed` written with `score: 0.72, label: "greed"`; run `SentimentAggregator.aggregate("BTC", ...)` with mocked Reddit (0.6) and Twitter (disabled stub 0.5) and FundingBias (0.01% → long); assert MemDir key `sentiment:composite:BTC` written; invoke `get_sentiment("BTC")`; assert `fearGreedScore: 0.72`, `compositeScore` within expected weighted range, resolved within 100 ms
- [x] T040 [P] Integration test — **FRED → `get_macro_context`**: mock FRED axios responses for all 4 series + CMC global stats; invoke `FredFeed.poll()`; assert `upsertMacroSnapshot` called with correct `effectiveDate`; call `buildMacroContext(pool, memDir)` — assert MemDir key `macro:snapshot` written; invoke `get_macro_context()`; assert `dxy` equals mocked FRED value, `regime` classified correctly; resolved within 200 ms
- [x] T041 [P] Integration test — **CryptoPanic → `get_news_events`**: mock axios returning 3 CryptoPanic results (2 unique, 1 duplicate); run `CryptoPanicFeed.poll(["BTC"])`; assert `insertNewsEvent` called exactly twice; assert `isDuplicate` called three times; invoke `get_news_events("BTC", 5)`; assert returned array has exactly 2 items with `symbols` including `"BTC"`, correct `impactClass` and `sentiment` from classifier
- [x] T042 [P] Integration test — **HealthMonitor stale feed**: set `sentiment:health:fear_greed` MemDir key with `lastSuccessfulPoll` 10 hours ago; invoke `HealthMonitor` worker job; assert `isStale: true`, `warn` log emitted containing `"fear_greed"`, and `pluginApi.alert` called with `alertChannelId`; update `lastSuccessfulPoll` to now; invoke monitor again; assert `isStale: false` and `info` log emitted (feed recovered)

---

## Dependencies

```
Phase 1 (Setup)
  └─► Phase 2 (Foundational — schemas, migration SQL, pg client, BullMQ queues)
        ├─► Phase 3 (US1 P1 — FearGreedFeed, RedditFeed, TwitterFeed, aggregator, get_sentiment)
        │     └─► Phase 4 (US2 P1 — FredFeed, MacroScheduler, regime classifier, get_macro_context)
        ├─► Phase 5 (US3 P2 — CryptoPanicFeed, classifier, deduplicator, get_news_events)
        │     └─► Phase 6 (US4 P2 — serializer, EmbeddingPipeline, pgvector upsert)
        │           └─► Phase 7 (US5 P3 — HealthMonitor, AccuracyScorer, get_feed_accuracy)
        └─► Phase 8 (Tool registration, plugin entry lifecycle, integration tests)
```

Phase 3 (US1) and Phase 5 (US3) can begin concurrently after Phase 2. Phase 4 (US2) depends on Phase 3 (the `regime` field in the composite snapshot uses `fearGreedScore`). Phase 6 (US4) depends on Phase 3 + Phase 5 being able to provide rows to embed. Phase 7 (US5) can begin after Phase 3 health keys exist, and the `AccuracyScorer` passively waits for the Phase 5 execution hook.

## Parallel Execution Examples

- T004, T005, T006, T007 can all run in parallel (different schema files, no cross-dependencies)
- T009 and T010 can run in parallel (pg client and BullMQ queue factory are independent)
- T011, T012, T013 can all run in parallel (different feed files, same IFeed interface)
- T014 and T015 depend on T011 completing (FearGreedFeed score needed by aggregator)
- T019 and T020 can run in parallel (MacroScheduler and regime classifier are independent)
- T024, T025, T026 can run in parallel (CryptoPanicFeed, classifier, deduplicator are separate files)
- T028 and T029 can run in parallel (serializer is consumed by EmbeddingPipeline, but both files can be written concurrently)
- T032, T033, T034 can run in parallel (HealthMonitor, AccuracyScorer, feed_accuracy queries are independent)
- T039, T040, T041, T042 integration tests can all run in parallel after their respective phases are complete

## Implementation Strategy

**MVP scope** (deliver US1 end-to-end as the first shippable increment):

1. T001–T003 (setup)
2. T004, T008, T009 (SentimentSnapshot schema + migration SQL + pg client)
3. T010 (BullMQ queues)
4. T011 (FearGreedFeed — simplest poll, no auth required)
5. T014, T015 (funding-bias + aggregator)
6. T016 (aggregator tests)
7. T017 (get-sentiment tool)
8. T036, T038 (tool registration + plugin entry — wires it all up)
9. T039 (smoke integration test against mocked alternative.me)

After MVP: complete US2 (FRED macro), then US3 (news/CryptoPanic), then US4 (embeddings), then US5 (health monitoring), then integration tests T040–T042. RedditFeed (T012) and TwitterFeed (T013) can be added alongside FearGreedFeed in the US1 sprint since they share the same poll pattern — only FearGreedFeed is strictly required for the MVP composite score.

The halt/safety dependencies from `001-advanced-context-memory` (MemDir typed keys, bounded-timeout reads) and `002-market-data-ingestion` (pg pool, funding rate keys written to `{exchange}:funding:{symbol}`) are consumed here without modification — both packages are runtime peer dependencies.
